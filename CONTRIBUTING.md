# 🤝 Руководство для контрибьюторов

Спасибо за интерес к улучшению Claude Code RU! Мы приветствуем любой вклад.

## 📋 Как помочь проекту

### 🐛 Сообщить об ошибке

1. Проверьте, нет ли уже подобной проблемы в [Issues](https://github.com/ваш-username/claude-code-ru/issues)
2. Создайте новый Issue с описанием:
   - Что вы делали
   - Что ожидали
   - Что произошло на самом деле
   - Версия Claude Code (`claude --version`)
   - Версия Node.js (`node --version`)
   - ОС и версия PowerShell

### 💡 Предложить улучшение

1. Откройте Issue с меткой `enhancement`
2. Опишите, что хотите улучшить и почему это полезно
3. Приведите примеры использования

### 🌐 Добавить переводы

Самый простой способ помочь проекту!

1. Откройте `src/claude-ru.cjs`
2. Найдите объект `translations`
3. Добавьте новые пары:
```javascript
const translations = {
  // Существующие переводы...

  // Ваши новые переводы
  'New English phrase': 'Новая русская фраза',
  'Another phrase': 'Другая фраза',
};
```

4. Протестируйте изменения
5. Создайте Pull Request

### 📖 Улучшить документацию

- Исправить опечатки
- Добавить примеры
- Улучшить объяснения
- Перевести на другие языки

## 🔧 Процесс разработки

### Настройка окружения

1. **Fork репозитория**
```bash
# Нажмите Fork на GitHub
```

2. **Клонируйте свой fork**
```bash
git clone https://github.com/ваш-username/claude-code-ru.git
cd claude-code-ru
```

3. **Установите зависимости**
```bash
npm install -g @anthropic-ai/claude-code
```

4. **Создайте ветку**
```bash
git checkout -b feature/my-new-feature
# или
git checkout -b fix/my-bug-fix
```

### Внесение изменений

1. **Редактируйте файлы**
   - `src/claude-ru.cjs` - основной файл перевода
   - `docs/` - документация
   - `examples/` - примеры

2. **Тестируйте изменения**
```bash
# Переустановите локально
.\scripts\claude-ru-reinstall.cmd

# Проверьте перевод
claude-ru --help

# Проверьте конкретную фразу
claude-ru --print "тест" | Select-String "нужная фраза"
```

3. **Коммитьте изменения**
```bash
git add .
git commit -m "feat: добавлен перевод для фразы X"
# или
git commit -m "fix: исправлена опечатка в переводе"
```

### Стиль коммитов

Используем [Conventional Commits](https://www.conventionalcommits.org/):

- `feat:` - новая функция
- `fix:` - исправление ошибки
- `docs:` - изменения в документации
- `style:` - форматирование, опечатки
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов
- `chore:` - обновление зависимостей, настройки

Примеры:
```bash
git commit -m "feat: добавлен перевод для MCP команд"
git commit -m "fix: исправлена замена слова 'no' внутри других слов"
git commit -m "docs: обновлена инструкция по установке"
```

## 📝 Требования к Pull Request

### Чеклист перед созданием PR

- [ ] Код протестирован локально
- [ ] Все новые переводы работают корректно
- [ ] Обновлена документация (если нужно)
- [ ] Коммиты следуют стилю проекта
- [ ] Нет конфликтов с main веткой

### Создание Pull Request

1. **Push в свой fork**
```bash
git push origin feature/my-new-feature
```

2. **Откройте PR на GitHub**
   - Перейдите в свой fork
   - Нажмите "Pull Request"
   - Заполните шаблон:

```markdown
## Описание
Что делает этот PR?

## Тип изменений
- [ ] Новые переводы
- [ ] Исправление ошибок
- [ ] Улучшение документации
- [ ] Новая функция

## Проверено
- [ ] Протестировано на Windows 10/11
- [ ] PowerShell 5.1 / 7.x
- [ ] Claude Code версия X.X.X

## Скриншоты (если применимо)
```

3. **Ожидайте ревью**
   - Отвечайте на комментарии
   - Вносите правки при необходимости

## 🧪 Тестирование

### Основные тесты

```powershell
# 1. Проверка справки
claude-ru --help | Select-String "Использование"
# Должно найти строку на русском

# 2. Проверка ошибок
claude-ru --invalid-option 2>&1 | Select-String "ошибка"
# Должно показать "ошибка: неизвестная опция"

# 3. Проверка конкретных фраз
claude-ru --help | Select-String "варианты"
# Должно найти переведенное слово "choices"

# 4. Проверка версии
claude-ru --version
# Должно показать версию Claude Code
```

### Тестирование новых переводов

1. Найдите английскую фразу в выводе:
```powershell
claude --help | Select-String "your phrase"
```

2. Добавьте перевод в `translations`

3. Переустановите:
```bash
.\scripts\claude-ru-reinstall.cmd
```

4. Проверьте перевод:
```powershell
claude-ru --help | Select-String "ваша фраза"
```

## 📐 Правила перевода

### Качество перевода

- ✅ Используйте общепринятые IT термины
- ✅ Сохраняйте смысл оригинала
- ✅ Будьте последовательны (одинаковые фразы - одинаковый перевод)
- ❌ Не используйте машинный перевод без проверки
- ❌ Не переводите технические термины (API, CLI, JSON и т.д.)

### Примеры хорошего перевода

```javascript
// ✅ Хорошо
'Error': 'Ошибка'
'Success': 'Успешно'
'Loading': 'Загрузка'
'unknown option': 'неизвестная опция'

// ❌ Плохо
'Error': 'Неполадка'  // Нестандартный термин
'API key': 'АПИ ключ'  // API не переводится
'default': 'дефолт'  // Используйте "по умолчанию"
```

### Длинные фразы

Для фраз с переменными сохраняйте переменные:

```javascript
// Оригинал
'Model for the current session.'

// Перевод
'Модель для текущей сессии.'

// С переменными (если есть)
'Using model: ${model}'
'Использование модели: ${model}'
```

## 🎯 Приоритеты

Что переводить в первую очередь:

1. **Высокий приоритет:**
   - Сообщения об ошибках
   - Справка основных команд
   - Часто используемые опции

2. **Средний приоритет:**
   - Справка подкоманд
   - Статусные сообщения
   - Интерактивные диалоги

3. **Низкий приоритет:**
   - Редко используемые опции
   - Технические детали
   - Debug сообщения

## 💬 Общение

- **Issues** - для багов и предложений
- **Discussions** - для вопросов и обсуждений
- **Pull Requests** - для code review

## 📜 Code of Conduct

Будьте вежливы и уважительны к другим участникам проекта.

## ❓ Вопросы?

Если у вас есть вопросы:
1. Проверьте [FAQ](docs/TROUBLESHOOTING.md)
2. Посмотрите существующие [Issues](https://github.com/ваш-username/claude-code-ru/issues)
3. Создайте новый Issue с вопросом

---

Спасибо за вклад в развитие проекта! 🙏
